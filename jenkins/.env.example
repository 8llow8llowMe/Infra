# Jenkins controller
JENKINS_WEB_PORT=49999
JENKINS_AGENT_PORT=50000
JENKINS_URL=http://jenkins-controller:8080
JENKINS_CONTROLLER_JAVA_OPTS=-Xms512m -Xmx2048m
JENKINS_OPTS=

# Jenkins builder agent
# Create this node in Jenkins UI first, then paste the generated secret here.
JENKINS_BUILDER_NAME=minipc-builder
JENKINS_BUILDER_SECRET=change-me-after-node-created

# Optional remote deploy-target agent example.
# Keep this here as a reference for backend-1, but this compose stack starts only the mini PC builder.
JENKINS_DEPLOY_AGENT_NAME=backend-1-deploy
JENKINS_DEPLOY_AGENT_SECRET=change-me-after-node-created
