# Vault image and port
VAULT_IMAGE=hashicorp/vault:1.18
VAULT_PORT=8200

# Replace the host/IP with the mini PC private IP or internal DNS name.
VAULT_ADDR=http://127.0.0.1:8200
VAULT_API_ADDR=http://127.0.0.1:8200
