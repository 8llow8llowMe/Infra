# bosspickseoul.com -> www.bosspickseoul.com 리다이렉트 (HTTP)
server {
    listen 80;
    server_name bosspickseoul.com;

    # Let's Encrypt challenge 파일 경로
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # 나머지는 www.bosspickseoul.com HTTPS로 리다이렉트
    location / {
        return 301 https://www.bosspickseoul.com$request_uri;
    }
}

# www.bosspickseoul.com HTTP -> HTTPS 리다이렉트
server {
    listen 80;
    server_name www.bosspickseoul.com;

    # Let's Encrypt challenge 파일 경로
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # HTTPS로 리다이렉트
    location / {
        return 301 https://$host$request_uri;
    }
}

# bosspickseoul.com -> www.bosspickseoul.com 리다이렉트 (HTTPS)
server {
    listen 443 ssl;
    server_name bosspickseoul.com;

    ssl_certificate /etc/letsencrypt/live/bosspickseoul.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/bosspickseoul.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    # www.bosspickseoul.com으로 리다이렉트
    location / {
        return 301 https://www.bosspickseoul.com$request_uri;
    }
}

# 메인 서버 설정 (www.bosspickseoul.com)
server {
    listen 443 ssl;
    server_name www.bosspickseoul.com;

    ssl_certificate /etc/letsencrypt/live/bosspickseoul.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/bosspickseoul.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    # Next.js SSR 서버로 프록시
    location / {
        proxy_pass http://web-ssr:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
